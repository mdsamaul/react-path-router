import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Products = () => {
   
    return (
        <div>
            <h2>this is Products pages</h2>
        </div>
    );
};

export default Products;